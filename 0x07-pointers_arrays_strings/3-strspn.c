#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * _strspn - function gets lengt of a prefix substring.
 * @s: str
 * @accept: char
 *
 * Return: int
 */
unsigned int _strspn(char *s, char *accept)
{
	unsigned int r = sizeof(accept);
	return (r);
}
