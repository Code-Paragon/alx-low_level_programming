#include "main.h"
/**
 * _abs - the function that computes the absolute value of an integer.
 * @n: the integer to be corrected
 *
 * Return: void it returns nothing
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else if (n < 0)
		return (-n);
	else
		return (n);
}
