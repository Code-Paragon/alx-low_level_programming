#include <stdio.h>
int main(void) {
	/**
	 * main - the main function this is used to print specific text with printf
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
